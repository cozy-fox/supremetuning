

# 🚀 How to Run This Full-Stack Project

Welcome! This guide will help you run this project.
Just follow the steps slowly and you'll succeed ✅

---

## 📌 Project Structure

This project contains 2 parts:

```

frontend  → What you see in the browser (React)
backend   → The server that makes everything work

````

You must run **both** at the same time using the same command:

```sh
npm run dev
````

---

## ✅ Step 1 — Install Required Program

### 👉 Install **Node.js (v22.15.0)**

1. Open your internet browser (Chrome, Edge, Safari, etc.)
2. Search for: **Node.js download**
3. Open the official website
4. Click **Download v22.15.0**
5. Install it like a normal application (*press Next → Install*)
6. Restart your computer after installation

---

## ▶️ Step 2 — Run the Backend (Server)

1. Open the **backend** folder
2. Right-click inside the empty space of that folder
3. Click one of these:

   * **Open in Terminal**
   * **Open PowerShell Here**
   * **Open Command Prompt**
4. Type the following and press **Enter**:

```sh
npm install
```

5. Wait until it finishes ✅
6. Then type:

```sh
npm run dev
```

🎯 The backend server is running now!
⚠️ **Do NOT close this terminal** — leave it open.

---

## ▶️ Step 3 — Run the Frontend (React App)

1. Now open the **frontend** folder
2. Right-click inside the folder
3. Click **Open in Terminal**
4. Run this:

```sh
npm install
```

5. Wait until it finishes ✅
6. Then start the app:

```sh
npm run dev
```

🎯 Frontend is running now!
⚠️ **Do NOT close this terminal** — leave it open too.

---

## 🌐 Step 4 — Open the App in Browser

After running the frontend, you will see a link like:

```
http://localhost:5173
```


* Hold the **CTRL (Windows) or CMD (Mac)** key and click it
  **OR**
* Copy the link and paste it into your browser address bar and open it

🎉 The app is now working!

---

## ✅ Checklist (Make sure all are done)

| ✓ Task                                                  |
| ------------------------------------------------------- |
| Node.js Installed (LTS)                                 |
| Computer Restarted                                      |
| Backend folder opened in terminal                       |
| `npm install` ran in Backend                            |
| `npm run dev` running in Backend (terminal still OPEN)  |
| Frontend folder opened in terminal                      |
| `npm install` ran in Frontend                           |
| `npm run dev` running in Frontend (terminal still OPEN) |
| `localhost` link opened in browser                      |

If everything above is done, you're all set ✅🎉

---

## ❌ If You See Errors

Try this recovery plan:

1. Close all Terminals
2. Open **backend** terminal again and run:

```sh
npm install
npm run dev
```

3. Open **frontend** terminal again and run:

```sh
npm install
npm run dev
```

4. Refresh the browser page

---
